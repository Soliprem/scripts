#!/usr/bin/env bash
setbg ~/.config/wpg/.current
sleep 2
if pgrep waybar; then
	waybar-restart &
fi
makoctl reload
if pgrep river; then
	river-theme
fi
notify-send -i "$HOME"/.config/wpg/.current "Changing theme..."
